configure:
#LA_TODO - zaza.openstack.charm_tests.vault.setup.auto_initialize_no_validation
#LA_TODO - zaza.openstack.charm_tests.keystone.setup.add_demo_user
#LA_TODO - zaza.openstack.charm_tests.nova.setup.create_flavors
#LA_TODO - zaza.openstack.charm_tests.nova.setup.manage_ssh_key
#LA_TODO - zaza.openstack.charm_tests.neutron.setup.basic_overcloud_network
 # LA_TEMP will fail unless: export TEST_HTTP_PROXY="http://squid.internal:3128"
 # but don't set that env var earlier as it will lead to keystone erroring
#LA_TODO - zaza.openstack.charm_tests.glance.setup.add_cirros_image
 - zaza.openstack.charm_tests.glance.setup.add_lts_image
tests:
#LA_TODO  - zaza.openstack.charm_tests.nova.tests.CirrosGuestCreateTest  # LA_TEMP
  - zaza.openstack.charm_tests.nova.tests.LTSGuestCreateTest
target_deploy_status:
  neutron-api-plugin-ovn:
    workload-status: waiting
    workload-status-message: "'certificates' awaiting server certificate data, 'ovsdb-cms' incomplete"
  ovn-central:
    workload-status: waiting
    workload-status-message: "'ovsdb-peer' incomplete, 'certificates' awaiting server certificate data"
  ovn-chassis:
    workload-status: waiting
    workload-status-message: "'certificates' awaiting server certificate data"
  vault:
    workload-status: blocked
    workload-status-message: Vault needs to be initialized
  ntp:
    workload-status: active
    workload-status-message: "chrony: Ready"
gate_bundles:
  - bundle
smoke_bundles:
  - bundle
